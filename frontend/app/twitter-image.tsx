// The Twitter card is the identical asset as the Open Graph card — re-export
// it rather than duplicate the JSX, so the two can never drift apart.
export { default, size, contentType, alt } from "./opengraph-image";
